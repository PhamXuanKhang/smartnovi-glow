version: '3.8'

services:
  smartnovi-web:
    build: .
    container_name: smartnovi-web
    restart: unless-stopped
    ports:
      - "127.0.0.1:3000:3000"
    environment:
      - NODE_ENV=production